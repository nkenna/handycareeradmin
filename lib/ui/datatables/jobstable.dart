import 'package:flutter/material.dart';

class JobDataTable extends StatelessWidget {
  const JobDataTable({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(),
    );
  }
}